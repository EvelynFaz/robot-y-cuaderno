
    lista OrdenadaNumeros(list <int>lista id)
    {
        while(id.count(i>0)
        var imenor = 0;
        for(i=0; i<id.count();i++)
        {
            if(llistaDesordenada[i]<listaDesordenada[imenor]),
            listaDesordenada.remove(imenor)
        }
        [imenor=i]
    }
    var intercam_id[i];
    id[i]=id[imenor]
    id[imenor]=interdcam.
        {
            return listaDesordenada;
        }
 