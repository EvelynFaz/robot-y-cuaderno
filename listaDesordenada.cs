class ordenador 
{
    [8,2,4];
    public list <int> ordenada(list int>lista desordenada)
    {
        while(listaDesordenada.ordenada.count()>0)
        var listaDesordenada = new List <int>();
        var imenor = 0;
        for(i=0; i<listaDesordenada.count();i++)
        {
            if(llistaDesordenada[i]<listaDesordenada[imenor]),
            listaDesordenada.remove(imenor)
        }
        {
            return listaDesordenada;
        }
    }
}
